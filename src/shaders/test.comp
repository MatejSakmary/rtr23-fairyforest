#version 450
#extension GL_GOOGLE_include_directive : require
#include "src/shared/shared.inl"

layout(push_constant, scalar) uniform pc {ComputeTestPC data;};

layout (local_size_x = COMPUTE_TEST_X_THREADS, local_size_y = 1, local_size_z = 1) in;
void main()
{
    ComputeTestBuffer(data.compute_test_buffer)[gl_LocalInvocationID.x].value = gl_LocalInvocationID.x;
}